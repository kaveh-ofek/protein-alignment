Simple script that takes two PDB identifiers and chain identifiers as input.
It will retrieve the appropriate files from the PDB and align the chains from each file by their set of C-alpha atoms,
including the calculation of the RMSD between these atoms.